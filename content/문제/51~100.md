- 55번: 프라이빗 서브넷에서 실행되는 EC2인스턴스만 RDS데이터베이스에 액세스
	- 프라이빗 서브네스이 인스턴스에 할당된 보안 그룹의 인바운드 트래픽을 허용하는 보안그룹을 DB에 연결
	- 기본이 거부이기에 EC2허용만 하는 규칙
- 57번: 이미지에 부적절한 콘텐츠 확인
	- Rekognition으로 부적절한 이미 감지
	- comprehend: 텍스트 정보 파악
- 62번: ALB에 외부 CA기관에서 발급한 SSL/TLS인증서를 사용하여 에지에서 암호화 매년 인증서 교체
	- AWS Certificate Manager(ACM)을 사용하여 SSL/TLS인증서를 가져오고 ALB에 적용 EventBridge(CloudWatch Eventes)를 사용하여 인증서가 만료될 때 알림을 보낸다.
	- 외부 인증기관 인증서기에 자동교체가 불가능하다.
- 67번: SQS대기열에서 메시지를 처리하고 RDS테이블에 쓰고 대기열에서 메시지를 삭제한다. RDS테이블에서 가끔 중복 레코드 발견 SQS는 중복 메시지 없음
	- ChangeMessageVisibility API호출을 사용하여 가시성 시간 초과를 늘린다.
	- 대기열의 메시지를 다른 사용자가 처리하지 못하게 막는 기간을 늘려야함(가시성 시간)
- 68번: 하이브리드 환경 짧은 지연시간과 고가용성 연결 필요 + 연결 실패시 더 느린 트래픽의 연결 필요(단일 장애 지점해소)
	- Direct Connect + VPN연결 프로비저닝
- 70번: NLB + HTTP애플리케이션 HTTP오류 감지
	- NLB를 ALB로 교체 HTTP상태 확인
	- NLB는 TCP/UDP계층 변수에만 의존 애플리케이션 가용성 보장 불가
- 73번: 사내 인터넷 연결을 통해 배스천 호스트 및 애플리케이션 서버에 연결해야함 모든 EC2인스턴스의 보안 그룹이 액세스 허용
	- 배스천 호스트: 외부 네트워크와 내부 네트워크를 연결하는 중간 서버
	- 배스천 호스트의 보안 그룹을 회사 외부IP(공인IP)범위에서 인바운드 액세스 허용 + 애플리케이션 인스턴스의 보안그룹을 배스천호스트의 개인 IP주소에서만 인바운드 SSH액세스 허용하는 보안 그룹으로 교체
	- 외부 IP => 번역이 조금 이상한데 퍼블릭 IP임
- 74번: 퍼블릭서브넷 EC2 + 프라이빗서브넷 EC2 Microsoft SQL Server 보안 그룹
	- 0.0.0.0/0에서 443의 인바운드 트래픽 웹 계층에서 허용
	- 웹 계층의 1433의 인바운드 트래픽을 데이터베이스 계층에서 허용
	- 인바운드: 기본적으로 차단 + 특정 트래픽만 허용
	- 아웃바운드: 기본적으로 허용 + 특정 트래픽이외 차단
- 79번: DynamoDB 아침에는 테이블 사용하지 않음 저녁에는 읽기및 쓰기 트래픽 예측 불가(빠르게 증가)
	- 온디맨드 용량모드에서 DynamoDB테이블 생성
	- 사용량을 모르기에 온디맨드 용량
- 80번: MSP파트너와 AWS계정과 AMI를 공유하는 가장 안전한 방법
	- AMI의 launchPermission속성을 추가 MSP파트너와 AWS계정과만 AMI를 공유 MSP파트너의 AWS계정이 키를 사용할 수 있도록 키 정책 수정
	- LaunchPermission: 해당 AMI로 인스턴스 시작할수있는 권한
- 82번: ACM으로 가져온 인증서를 만료 30일 전에 알림
	- 인증서 확인하는 Config규칙 생성 SNS + EventBridge로 알림
- 88번: 미국의 S3에 버킷을 유럽과 공유, 전송비용 최적화
	- S3버킷의 요청자 지불 기능 구성
	- 버킷 소유자가 아닌 요청자가 비용 부담하게됨
- 93번: MySQL온프레미스 마이그레이션 개발팀은 4시간마다 프로덕션 데이터베이스의 전체를 내보내 준비환경의 데이터베이스를 채움 이시간동안 사용자 대기시간 길어짐, 지연없이 스테이징 환경 사용
	- 프로덕션용 다중 AZ Aurora복제본과 함께 Aurora MySQL사용 데이터베이스 복제를 사용하여 요청 시 스테이징 데이터베이스 생성
- 98번: SQS메시지가 Lambda함수를 두번 이상 호출
	- SQS대기열의 가시성 제한 시간을 함수 제한 시간과 일괄 처리 창 제한 시간의 합계보다 큰 값으로 늘린다.
	- 메시지 가시성 제한(다른 사용자가 사용하지 못하게)을 늘린다.






