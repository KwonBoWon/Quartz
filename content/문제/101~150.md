- 101번: 퍼블릭 및 프라이빗 서브넷이 있는 VPC에 인터넷 게이트웨이가 있다. 3개의 가용영역에서 프라이빗 서브넷에 대한 인터넷 액세스를 활성화 하는 방법
	- 각 AZ의 퍼블릭 서브넷에 하나씩 NAT게이트웨이를 생성한다. VPC가 아닌 트래픽을 해당 AZ의 NAT게이트웨이로 전달하는 각 AZ에 대한 프라이빗 라우팅 테이블을 생성한다.
- 103번: Glue가 오래된 데이터를 재처리하지 못하도록 하려면 어떻게 해야 합니까?
	- 작업 북마크(job bookmarks)를 사용하도록 작업을 편집합니다.
- 105번: EventBridge규칙이 Lambda함수를 호출하기 위한 요구사항(최소권한)
	- lambda:InvokeFunction을 작업으로, Service: events.amazonaws.com 을 보안 주체로 사용하여 리소스 기반 정책을 함수에 추가한다.
	- Lambda의 전체 기능이 아닌 함수 호출기능(lambda:InvokeFunction)만 사용하도록 하고, 전체 보안 주체가 아닌(*) 아마존 이벤트 서비스만 보안 주체로 사용
- 108번: RDS에 자동차 모록 저장하는 판매 웹사이트 자동차가 판매되면 웹사이트에서 목록을 제거해야 하고 여러 대상 시스템으로 보내야한다.
	- RDS이벤트 알림을 구독하고  fanned out된 SNS주제를 여러 SQS대기열로 보낸다. Lambda를 잉요하여 업데이트 한다.
	- SNS+SQS fanned out: 하나의 메시지를 여러 대상으로 동시에 전송하는 아키텍쳐이다.
- 110번: 사용자가 웹사이트에 이미지를 업로드 한다. 업로드 요청중 웹사이트는 이미지의 크기를 표준 크기로 조정하고 크기가 조정된 이미지를 S3에 저장한다. 사용자가 느린 업로드 요청을 경험중, 커플링을 줄이고 웹사이트 성능을 개선해야 한다
	- 미리 서명된 URL을 사용하여 사용자의 각 브라우저에서 S3로 이미지를 직접 업로드 + 이미지가 업로드 될 때 Lambda함수를 호출하도록 S3이벤트 알림을 구성해 이미지 크기 조정
	- 미리 서명된 URL을 통해 웹사이트에 업로드하지 않고 바로 업로드 가능하다.
- 116번: 웹사이트에 콘텐츠 관리 시스템(CMS)를 사용한다. 1년에 4번 업데이트되며 동적 컨텐츠가 필요하지 않다. 높은 확장성과 보안을 필요
	- HTTPS기능으 사용하도록 CloudFront + 새 웹사이트를 정적 웹사이트 호스팅이 활성화된 S3버킷에 배포
- 120번: 2개의 리전에 3개씩의 EC2인스턴스가 있음 자체 관리형 DNS있음, 트래픽을 모든 EC2인스턴스로 라우팅
	- Global Accelerator에서 포준 액셀레이터로 생성 두 리전을 엔드포인트 그룹으로 생성 엔드포인트  그룹에 NLB추가
- 121번: RDS의 데이터베이스와 스냅샷이 앞으로 항상 암호화되도록 하기
	-  최신 DB스냅샷 사본을 암호화한다 암호환 스냅샷을 복원하여 기존 DB인스턴스를 교체한다.
- 127번: 비디오 처리를 위한 최대 성능을 갖춘 10TB스토리지, 미디어 컨텐츠 저장를 위한 300TB의 매우 내구성 있는 스토리지, 아카이브를 위한 9000TB의 스토리지
	- 최고의 성능을 위한 EC2인스턴스 스토어, 내구성 있는 스토리지를 위한 S3스토리지, 아카이브 스토리지를 위한 S3 Glacier
- 131번: S3버킷을 스토리지로 사용할 파일 공유 애플리케이션 CloudFront 배포를 통해 모든 파일을 제공하는데 S3 URL에 대한 직접 탐색을 통해 파일에 액세스하는 것을 원하지 않는다
	- 원본 액세스 ID(OAI)를 생성한다. CloudFront 배포에 OAI를 할당한다. OAI만 읽기 권한을 갖도록 S3버킷 원한을 구성한다.
	- S3 + CloudFront를 사용하는 경우 S3직접 액세스를 막으려면 OAC(S3뿐만아니라 다른 리소스도 보호)또는  OAI를 사용하면된다.
- 133번: 온프레미스에서 Oracle데이터베이스를 실행한다. AWS로 마이그레이션 하는 과정에서 데이터베이스를 사용 가능한 최신 버전으로 업그레이드하려고 한다. DR을 설정하고 기본 운영체제에 대한 액세스를 유지 관리해야 한다.
	- Oracle용 RDS Custom으로 마이그레이션하고 다른 리전에 읽기 전용 복제본을 생성한다.
	- 기본 운영체제에 대한 액세스 유지 => RDS Custom
- 134번: 애플리케이션을 서버리스로 이동 SL을 이용해여 데이터 분석 S3에 저장 데이터는 암호화되어야 하고 다른 AWS리전에 복제
	- 기존 S3에 데이터 로드 S3교차 리전 복제(CRR)로 암호화된 객체를 다른 리전의 S3버킷에 복제한다 S3관리형 암호화 키(SSE-S3)로 서버측 암호화를 사용한다 Athena를 사용하여 데이터를 쿼리한다.
- 135번: 외부 공급자의 서비스에 연결해야 한다. 공급자의 VPC와 회사의 VPC와 비공개로 연결해야하고 대상 서비스로 제한되어야 한다. 연결은 회사의 VPC에서만 시작되어야 한다.
	- 공급자에게 대상 서비스에 대한 VPC엔드포인트를 생성하도록 요청한다. PrivateLink를 사용하여 대상 서비스에 연결한다.
	- 특정 서비스 API연결 => PrivateLink
- 141번: 정적 콘텐츠와 동적 콘텐츠를 제공, ALB뒤의 EC2인스턴스에서 실행되는 API서버를 통해 HTTPS를 통해 제공된다 가능한 빨리 전 세계 사용자에게 제공되기를 바란다.
	- CloudFront를 사용하여 ALB를 오리진으로 지정하여 모든 정적 및 동적 콘텐츠 제공
	- ALB를 오리진으로 설정할 수 있다.
- 142번: UDP기반 트래픽 애플리케이션 대기 시간이 짧고 가장 가까운 엣지 로케이션으로 트래픽을 라우팅하고 애플리케이션 엔드포인트에 진입하기 위한 고정 IP주소를 제공해야 한다.
	- NLB로 요청을 전달하도록 Global Accelerator구성한다. EC2 Auto Scaling 그룹의 애플리케이션에 EC2인스턴스를 사용한다.
	- UDP + 고정 IP주소 => Global Accelerator
- 150번: 인프라 메트릭 경보를 구현해야 합니다. CPU사용률이 단기간에 50%이상으로 증가하는 경우 회사는 조치를 취할 필요가 없다. CPU사용률이 50%이상 증가하고 IOPS가 동시에 높다면 최대한 빨리 조치를 취해야 한다.
	- 인프라 메트릭 경보 => CloudWatch(전체 애플리케이션 또는 리소스 그룹에 대한 요약되고 집계된 상태 지표 생성 가능)
	- CloudWatch 복합 경보를 생성한다.














