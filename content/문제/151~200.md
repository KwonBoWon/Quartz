- 151번: ap-northeast-3 지역만 사용할 수 있고 VPC를 인터넷에 연결할 수 없다.
	- Control Tower를 사용하여 데이터 상주 가드레일(SCP)을 구현하여 인터넷 액세스를 거부하고 ap-n-3를 제외한 모든 리전에 대한 액세스 거부
	- Organizations를 사용하여 VPC가 인터넷에 액세스하지 못하도록 하는 서비스 제어 정책(SCPS)을 구성한다. ap-n-3를 제외한 모든 리전에 대한 액세스 거부
- 157번: Aurora PostgreSQL에 데이터를 5년간 보관하고 5년이 지나면 모든 데이터 삭제, 작업의 감사 로그를 무기한으로 유지, Aurora용 자동 백업 구성 완료
	- DB클러스터에 대한 AmazonCloudWatch Logs내보내기를 구성 + AWS Backup을 사용하여 백업을 수행 5년동안 백업 보관
- 162번: 고성능 컴퓨팅(HPC)인프라 사용, 수백개의 EC2스팟 인스턴스에서 실행중, 모든 EC2인스턴스에서 데이터를 처리할 수 있게 영구 스토리지로 복사할 수 있는 클라우드 스토리지 솔루션
	- S3와 통합된 FSx for Lustre
- 165번: 정적 웹 사이트 저장 S3오리진과 함께 CloudFront 모든 트래픽을 WAF에서 검사한다.
	- 원본 액세스 ID(OAI)를 사용하여 S3버켓에 대한 액세스를 제한하도록 CloudFront및 S3를 구성한다 배포에서 WAF를 활성화한다.
	- CloudFront는 S3오리지에 인증된 요청을 전송하기 위해서 OAC/OAI제공
- 168번: AWS계정에서 특정 서비스 또는 작업에 대한 액세스를 제한하려고 한다. Organizations의 대규모 속한다. 확장가능하며 권한을 유지할 수 있는 단일 지점이 있어야 한다.
	- 루트 조직 단위 서비스 제어 정책(SCP)을 만들어 서비스 또는 작업에 대한 액세스를 거부한다. => 단일 지점
- 170번: ALB뒤의 EC2에서 특정 국가에서만 애플리케이션 액세스
	- VPC의 ALB에서 WAF를 구성한다.
- 172번: CloudFront 배포중 민감한 정보를 애플리케이션 스택에서 보호해야 하고 정보에 대한 액세스는 특정 애플리케이션으로 제한되어야한다.
	- CloudFront 필드 수준 암호화 프로필 구성
	- 필드 수준 암호화는 특정 애플리케이션만 볼 수 있도록 시스템 처리 전반에 걸처 특정 데이터를 보호할 수 있는 추가 보안계층 추가함
- 175번: API GW + Lambda + Aurora PostgreSQL 고객주문 급증 시간 초과, DB CPU사용률과 메모리 사용량이 높음
	- RDS프록시를 사용하여 데이터베이스에 대한 프록시를 생성한다. 데이터베이스 엔드포인트 대신 RDS 프록시 엔드포인트를 사용하도록 Lambda 함수를 수정한다.
	- RDS프록시는 데이터베이스 연결 풀을 설정해 풀에서 연결 재사용(연결을 여는 오버헤드 방지)
- 177번: DynamoDB를 사용하여 메타데이터를 저장하고 있다 성능을 효율성을 개선해야한다.
	- DynamoDB Accelerator(DAX) 
- 179번: RDS인스턴스에 액세스 하는 사용자 이름과 암호를 안전하게 저장해야한다. Systems Manager Parameter Store에서 보안 매개변수를 생성하려고 한다.
	- Parameter Store파라미터에 대한 읽기 액세스 권한이 있는 IAM역할을 생성한다 파라미터를 암호화하는 데사용되는 KSM키에대한 Decrypt액세스 허용 이 역할을  EC2인스턴스에 할당한다.
- 180번: NLB + EC2 + API GW, SQL인젝션과 같은 웹 익스플로잇으로부터 플랫폼 보호 DDoS공격 감지 완화
	- NLB와 함꼐 Shield Advanced사용
	- WAF를 API GW보호
- 185번: ECS + S3 API호출, 애플리케이션이 S3에 액세스할 권한이 있는지 어떻게 확인할 수 있나요
	- S3권한이 있는 IAM역할을 생성한 다음 작업 정의에서 해당 역할을 taskRoleArn으로 지정한다.
- 194번: EC2중단 이벤트 발생시 자동으로 장애 조치
	- 동일한 AWS지역의 다른 가용 영역에서 두개의 EC2인스턴스 시작 EC2인스턴스를 클러스터로 구성한다.
- 196번: EC2 + DynamoDB 애플리케이션에는 지난 30일동안의 데이터만 필요하다
	- 애플리케이션을 확장하여 현재 타임스탬프에 30일 더한 값을 테이블에 생성된 각 새 항목에 추가하는 속성을 추가한다 속성을 TTL속성으로 사용하도록 DynamoDB를 구성한다.
- 








