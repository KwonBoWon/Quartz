
- 2번: S3 로그 분석 쿼리
	- Athena
- 3번: S3 버킷에 대한 액세스를 Organization 조직 내 계정 사용자로만 제한
	- PrincipalOrgID로 참조, PrincipalOrgPaths는 다중 조건
- 4번: ==S3에 대한 프라이빗 네트워크 연결을 제공하는 솔루션==
	- S3버킷에 대한 게이트웨이 VPC 엔드포인트 생성
	- 게이트웨이 엔드포인트: S3, DynamoDB는 무료
	- 인터페이스 엔드포인트: AWS내부망으로 전달 비용 부과
- 11번: EC2가 Aurora에 자격증명
	- AWS Secrets Manager, 암호 자동 변경
- 12번: ALB + S3 + Route53 정적및 동적 데이터 성능, 대기시간 개선
	- S3와 ALB를 오리진으로 포함하는 CloudFront배포, CloudFront 배포로 트래픽 라우팅하게 Route53구성
	- CloudFront는 오리진을 S3, MediaStore, ALB, Lambda등 설정 가능
- 15번: VPC 트래픽 보호하는 솔루션, 트래픽 흐름 검사, 필터링 필요
	- Network Firewall사용
	- GuardDuty는 계정 및 워크로드 보호
- 17번: EC2가 S3에 액세스
	- S3버킷에 대한 액세스 권한을 부여하는 IAM 역할을 EC2에 연결
	- 역할은 능동적, 정책은 수동적 => EC2에 연결하므로 역할
- 19번: 트래픽이 웹 서버에 도달하기전에 모든 트래픽을 검사하기 위해 웹 애플리케이션을 어플라이언스와 통합
	- 게이트웨이 로드 밸런서 배포
	- 게이트웨이 로드 밸런서는 VPC트래픽 제어 보안 어플라이언스 통합
- 20번: EBS볼륨의 프로덕션 데이터 복제
	- EBS볼륨의 스냅샷을 만들어 새 EBS볼륨에 복원
	- EC2인스턴스 스토어 볼륨은 휘발성이다.
- 24번: EC2비용을 비교하는 그래프를 생성하고 심층 분석
	- Cost Explorer의 세분화된 필터링 기능으로 인스턴스 유형을 기반으로 EC2비용에 대한 심층 분석 수행
- 28번: Organization에 SSO솔루션 + Microsoft Active Directory에서 사용자 및 그룹 관리
	- SSO: 한번의 로그인으로 여러 애플리케이션및 서비스 접근
	- AWS SSO활성화, AWS Directory Service사용하여 Microsoft Active Directory와 AWS SSO를 연결하는 양방향 forest trust 생성
- 30번: RDS에서 매월 테스트 실행(유일한 프로세스)
	- 테스트가 완료되면 스냅샷 생성하고 인스턴스 종료
- 31번: 모든 EC2, RDS, Redshift에 태그를 검사하고 구성
	- Config 규칙을 사용하여 적절하게 태그가 지정되지 않은 리소스를 정의하고 감지
- 33번: 수십만명의 사용자의 거래 세부정보를 여러 내부 애플리케이션과 공유할 수 있는 확장 가능한 거의 실시간 솔루션 + 민감한 데이터 트랜잭션
	- 트랜잭션 데이터를 Kinesis Data Streams으로 스트림 Lambda통합으로 민감한 데이터 제거한 뒤 DynamoDB에 저장
	- 수십만명의 실시간 솔루션: Kinesis Data Streams
	- Kinesis Data Firehose는 데이터 변환및 전송 서비스
- 36번: 고객 관리형 키를 사용하여 S3버킷 데이터 암호화
	- 클라이언트 측 암호화(서버측 암호화 SSE-S3는 고객관리형 키사용 하지 않음)
	- SSE-C는 고객키로 암호화
- 43번: S3에 백업하는 온프레미스 애플리케이션의 인터넷 연결 영향 최소화
	- Direct Connect 연결 설정(전용선 통과)
	- VPN은 인터넷 통과
- 44번: S3 우발적인 삭제 보호
	- 버전관리, MFA삭제 활성화
- 46번: SFTP를 통해 200GB초과 데이터 분석 + PII포함시 알림
	- S3를 보안 전송 지점으로 사용 Macie를 사용하여 버킷 객체 스캔 SNS로 알림
- 47번: 1주일동안의 이벤트를 위해 3개의 가용영역의 보장된 EC2용량 필요
	- 3개의 가용영역을 지정하는 온디맨드 용량 예약
	- 예약 인스턴스는 1년혹은 3년만 가능
- 49번: 1년 이내 무작위로 자주 액세스 1년 이후에는 자주 액세스 하지 않음
	- S3 Intelligent-Tiering에 저장 수명 주기 정책으로 1년후 Glacier Flexible Retrieval로 이동 Athena를 이용하여 S3파일 쿼리 S3 Glacier Select를 사용하여 Glacier에 있는 파일 쿼리
- 50번: 1000개의 EC2 빨리 모든 EC2에서 타사 소프트웨어 패치
	- AWS System Manger Run Command를 사용하여 모든 인스턴스에 패치를 적용








